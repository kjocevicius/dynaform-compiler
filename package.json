{
  "name": "dform-compiler",
  "version": "0.0.2",
  "author": "Karolis Jocevičius",
  "description": "Completes form json object with referenced items",
  "keywords": [
    "forms",
    "dynamic",
    "dynaform",
    "angular",
    "ng"
  ],
  "license": "GPL-3.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "test": "jasmine-ts",
    "build": "tsc",
    "start": "npm run build && node dist",
    "prepare": "npm run build",
    "prepublishOnly": "npm run lint && npm run test"
  },
  "dependencies": {
    "@types/node": "^9.3.0",
    "dform-model": "0.0.2",
    "typescript": "^2.6.2"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.4",
    "jasmine": "^2.9.0",
    "jasmine-ts": "^0.2.1",
    "tslint": "^5.9.1"
  }
}
